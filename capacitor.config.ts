import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tech.zhangjc.cook',
  appName: '居家食用手册',
  bundledWebRuntime: false,
  webDir: 'out'
};

export default config;
